target/

# Secrets — never commit
.env
.env.*
!.env.example
k8s/secret.yaml

# Python bytecode from running the scripts/jenkins-*.py helpers locally
__pycache__/
*.pyc


# CodeGraph local index (added 2026-06-25)
# CodeGraph local index — generated by `codegraph init`
# Add to your project's .gitignore
.codegraph/

# Optional: keep the empty .codegraph/ in repo for shared setup hints
# Uncomment the next 2 lines to keep the directory but ignore its contents
# !.codegraph/
# !.codegraph/.gitkeep
